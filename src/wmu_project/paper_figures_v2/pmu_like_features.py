"""Implementation notes: v2 logic is orchestrated by report.py.

This lightweight module documents the requested package boundary and keeps
imports stable for tests/readers. The generation functions are grouped in
report.py to avoid duplicating state during the first reproducible v2 run.
"""
